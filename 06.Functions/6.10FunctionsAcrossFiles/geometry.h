#pragma once   // include guard: this header's contents are pasted at most
               // once per translation unit, no matter how many #includes

// DECLARATIONS only - the signature and a ';', no body. This is the
// "promise" a caller compiles against. The bodies live in geometry.cpp.

double circle_area(double radius);
double circle_circumference(double radius);
