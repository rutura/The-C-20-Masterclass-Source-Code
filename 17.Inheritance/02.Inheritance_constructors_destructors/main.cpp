/*
    . Inheritance and constructors
        .#1: Default arg constructors with inheritance

        .#2: Constructors and inheritance

        .#3: Copy constructors and inheritance

        .#4: Inheriting base constructors

    . Build instructions: 
        . Bare hands with CMake: 
            . cmake -S . -B build
            . cmake --build build

        . With CMake presets (VS Code): 
            . Select configure preset
            . Select build preset
            . Use the run and debug buttons: We can debug application right from the start.

        . Can also use presets on the command line.
    . Dependencies: 
        . gcc
        . cmake
        . ninja
        . vcpkg and setting up the VCPKG_ROOT environment variable
        . gdb any version

*/

// One line comment
#include "utilities.h"

int main() {
  //inheritance_with_default_arg_constructors();
  //constructors_and_inheritance();
  //copy_constructors_and_inheritance();
  //inheriting_base_constructors();
  //inheritance_and_destructors();

}