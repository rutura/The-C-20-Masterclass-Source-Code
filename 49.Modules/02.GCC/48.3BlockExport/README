Compilation: 

    . To be able to import standard headers like iostream and string, we need to build a module interface unit for each of them. Thats what the commands #1 and #2 do. After that, we just build the module interface unit for math.ixx and compile it to an object file. Finally, we link the object file with the main.cpp file to get the executable.

    . Commands: 
        1. g++ -fmodules-ts -x c++-system-header string
        2. g++ -fmodules-ts -x c++-system-header iostream
        3. g++ -fmodules-ts -c -x c++ .\math.ixx
        4. g++ -fmodules-ts main.cpp math.o -o rooster.exe
